# dev 빌드/개발서버(vite dev, --mode development)에서 사용 — dev PublicApi
VITE_API_BASE=https://dev-api.barunsoncard.com/api
# 시크릿은 커밋하지 않음. dev 테스트는 브라우저 localStorage('LOUNGE_SECRET') 주입 또는
# 빌드 환경변수 VITE_LOUNGE_SECRET 로 주입.
